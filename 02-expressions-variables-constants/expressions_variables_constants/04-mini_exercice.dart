import 'dart:math';

void main() {
  print(sin(45 * pi / 180));
  print(sqrt(2));
}

// Converting 45° to radians and then using
// the Dart sin function, which works in radians, to
// calculate the result. Now print the value of 1 over
// the square root of 2 in Dart. Confirm that it
// equals the sine of 45°.